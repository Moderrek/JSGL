import GameEvent from '@/events/GameEvent';

/**
 * Event emitted when the game starts.
 * Contains a reference to the game instance.
 * @group Game Events
 */
export type GameStartEvent = GameEvent;
